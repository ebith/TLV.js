extends layout

block script
  != js('index.js')

block content
  #navigation
    form#pager.form-inline
      button#load-older.btn.pull-left(type='button') 古いの
      input#search.input-xlarge(type='text', placeholder='検索する？')
      button#load-recent.btn.pull-right(type='button') 最新の
  #message-container
